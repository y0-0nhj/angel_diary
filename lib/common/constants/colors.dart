import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF8F5EF);
  static const Color primary = Color.fromRGBO(164, 216, 117, 1);
  static const Color secondary = Color(0xFFB0B0B0);
  static const Color text = Color(0xFF3D3D3D);
  static const Color cardBackground = Colors.white;

  // 추가 색상
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // 투명도가 있는 색상
  static Color overlay = Colors.black.withOpacity(0.5);
  static Color cardBackgroundTransparent = Colors.white.withOpacity(0.9);
}
